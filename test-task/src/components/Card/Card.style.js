import { BASE_GRAY, BASE_WHITE, SHADOW_BLACK } from "../../styles/colors";

export const style = {
  cardWrapper: {
    background: BASE_WHITE,
    border: `1px solid ${BASE_GRAY}`,
    borderRadius: "8px",
    padding: "16px",
    boxShadow: `0 2px 8px ${SHADOW_BLACK}`,
  },
};
